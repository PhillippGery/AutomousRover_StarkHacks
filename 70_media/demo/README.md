# Demo Videos

Add demo videos and GIFs here during and after StarkHacks 2026.

Recommended format: `.mp4` (H.264), max 100MB per file. Use Git LFS for larger files.

```bash
git lfs track "*.mp4"
git lfs track "*.gif"
```
