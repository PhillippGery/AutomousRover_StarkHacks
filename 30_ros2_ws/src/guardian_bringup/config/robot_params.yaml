guardian:
  wheel_radius: 0.048      # meters — JGB37-3530 with mecanum wheel
  wheel_base_length: 0.25  # meters — front to back, measure on chassis after build
  wheel_base_width: 0.20   # meters — left to right, measure on chassis after build
  max_rpm: 300.0
  serial_port: /dev/ttyACM0
  serial_baud: 115200
