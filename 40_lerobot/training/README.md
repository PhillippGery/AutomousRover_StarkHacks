# Training

**Owner: Dipam**

Training logs, scripts, and checkpoints go here.

After recording demonstration datasets, train a policy using:

```bash
# TODO: fill in actual LeRobot training command at hackathon
# python -m lerobot.scripts.train \
#   --config 40_lerobot/configs/guardian_policy_config.yaml \
#   --output_dir 40_lerobot/training/run_001
```

Add trained checkpoint folders here (`.pt` files) — use Git LFS if files are large.
